{
  "name": "hubspace-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Dev-time UI source for hubspace. Built output lands in ../static and ships in the wheel; this package never ships.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
